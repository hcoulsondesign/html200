// console.log('it is working');

function square() {
  let input = prompt('Enter a number');
  let result = 0;

  result = Number(input) * Number(input);

  alert(result);
}

function multiply() {
  let input1 = prompt('Enter your first number');
  let input2 = prompt('Enter another number');
  let product = 0;

  product = Number(input1) * Number(input2);

  alert(product);
}
