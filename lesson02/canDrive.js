const first = 'Margaret';
const last = 'Thomas';
const drivingAge = 16;
let driverAge = 35;
let diff = driverAge - drivingAge
if (diff >= 0) {
  console.log (`${first} ${last} can drive. He/she/they have been driving for ${diff} years.`)
} else
  for (i=5; i>=0; i--) {
    console.log(i);
}
